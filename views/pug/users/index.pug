extends ../layouts/base

block content
  h2 User List
  ul.user-list
    each user in users
      li.user-item
        a(href=`/users/${user.id}`)= user.name
        p Email: #{user.email}
        p Age: #{user.age}